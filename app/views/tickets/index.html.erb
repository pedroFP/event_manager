<h1>Tickets</h1>
<table class="table table-hover table-responsive">
  <thead>
    <tr>
      <th>Evento</th>
      <th>Precio</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @tickets.each do |ticket| %>
      <tr>
        <td><%= ticket.event.name %></td>
        <td><%= ticket.event.price %></td>
        <td><%= link_to 'Ver', ticket, class: 'btn btn-primary' %></td>
        <td><%= link_to 'PDF', '#', class: 'btn btn-primary' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
