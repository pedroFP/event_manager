<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'Event Manager', root_path, class: "navbar-brand active" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if user_signed_in? %>
      <ul class="navbar-nav navbar-right mr-auto">
        <li class="nav-item">
          <%= link_to 'Eventos', events_path, class: 'nav-link active' %>
        </li>
        <% if current_user.admin? %>
          <li class="nav-item">
            <%= link_to 'Usuarios', users_path, class: 'nav-link active' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Mis Tickets', tickets_path, class: 'nav-link active' %>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            <%= link_to 'Editar Perfil', edit_user_registration_path, class: 'dropdown-item' %>
          </div>
        </li>
      </ul>
    <% end %>
  </div>
</nav>
